[project]
name = "falcoria-logging"
version = "0.1.0"
description = "Shared process-wide logging configuration for falcoria services."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/falcoria_logging"]
